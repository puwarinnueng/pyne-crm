// authConfig.js — username/password สำหรับรันบนเครื่อง (localhost/mock) เท่านั้น
// Production (gas/) ไม่ใช้ไฟล์นี้ — ค่าจริงเก็บในแท็บ Config ของ Sheet แทน (ดู gas/Config.gs, gas/Setup.gs)
export const AUTH_CONFIG = {
  username: "admin@gmail.com",
  password: "pyne1234"
};
